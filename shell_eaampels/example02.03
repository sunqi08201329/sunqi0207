#!/bin/sh 
file=./example02.03
echo "test file is" $file
if [ -f $file ]
  then
	echo file exists
fi
 
if [ -d $file ] 
  then
 	 echo file is a directory
fi

if [ -s $file ] 
  then
	 echo file is not of zero length 
fi

if [ -r $file -a -w $file ]
  then
	 echo file is readable and writeable
fi
